{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug (gdb) archivo actual",
      "type": "cppdbg",
      "request": "launch",
      "program": "${fileDirname}\\build\\${fileBasenameNoExtension}.exe",
      "args": [],
      "cwd": "${workspaceFolder}",
      "environment": [],
      "MIMode": "gdb",
      "miDebuggerPath": "C:/msys64/ucrt64/bin/gdb.exe",
      "externalConsole": true,           // <- usa una ventana de consola (cmd)
      "preLaunchTask": "C++: build active file",
      "setupCommands": [
        { "description": "Habilitar impresión bonita", "text": "-enable-pretty-printing", "ignoreFailures": true }
      ]
    }
  ]
}
